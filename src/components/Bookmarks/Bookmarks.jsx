import PropTypes from 'prop-types';

const Bookmarks = () => {
    return (
        <div>
            
        </div>
    );
};

Bookmarks.propTypes = {
    bookmarks:PropTypes.object.isRequired
}

export default Bookmarks;